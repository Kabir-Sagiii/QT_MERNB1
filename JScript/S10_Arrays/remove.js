//pop
var info = [100, 200, 300, 400, 500, 600, 700];

console.log(info);
info.pop();
info.pop();
var removedElement = info.pop();
console.log(info);
console.log(removedElement);

//shift()
info.shift();
console.log(info);
